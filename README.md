# Tim and Burke BLE Lab
Implemented a temperature service that pulls temperature from the pico and sends it to the client. In order to match the Thingy52 Temperature format we needed to flip the little endian sending format. 
